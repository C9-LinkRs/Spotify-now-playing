version: '3.7'

services:
  now_playing:
    container_name: now_playing
    build: ./
    restart: always
    env_file: .env
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    ports:
      - 3000:3000
  
volumes:
  node_modules: